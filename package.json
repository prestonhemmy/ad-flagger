{
  "name": "ad-flagger",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:build-bundle": "vite build --config vite.config.test-bundle.ts",
    "test:integration": "npm run test:build-bundle && npx playwright test",
    "test:integration:ui": "npm run test:build-bundle && npx playwright test --ui",
    "test:integration:headed": "npm run test:build-bundle && npx playwright test --headed",
    "test:integration:debug": "npm run test:build-bundle && npx playwright test --debug",
    "test:all": "npm run test:unit && npm run test:integration",
    "playwright:report": "npx playwright show-report"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "^0.2.81",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@tailwindcss/vite": "^4.1.18",
    "@types/chrome": "^0.0.304",
    "@types/node": "^25.2.1",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^28.1.0",
    "serve": "^14.2.6",
    "tailwindcss": "^4.1.18",
    "typescript": "^5.7.0",
    "vite": "^6.1.0",
    "vitest": "^4.0.18"
  }
}
